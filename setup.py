from setuptools import find_packages, setup

setup(name='mcdc',
      description='Implement 2d / 3d Marching Cubes and Dual Contouring.',
      keywords='3D isosurface',
      #  packages=find_packages(),
      packages=['mcdc'],
      classifiers=[],
      zip_safe=False)
